export type CreateServiceOrderProps = Record<string, never>;

export type Vehicle = {
  id: string;
  label: string;
};

export type Mechanic = {
  id: string;
  name: string;
  specialty: string;
  rating: number;
};
